// No Payload config here — this section is driven entirely by the Services
// collection, which `payload.config.ts` already registers by path.
export { Metiers } from './Component'
export { adaptMetiers } from './adapt'
export type { MetiersProps, Metier, MetierPhoto } from './types'
